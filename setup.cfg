[flake8]
exclude=env*,.tox,.git,*.egg,build

[tool:pytest]
testpaths = tests
addopts = --cov=dtool_lookup_server_retrieve_mongo_plugin
#addopts = -x --pdb

[cov:run]
source = dtool_lookup_server_retrieve_mongo_plugin
